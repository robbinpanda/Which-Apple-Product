# 苹果产品对比 · 大修改报告

数据快照：2026-08-17

## 1. 本次结果

- 产品范围由 2020 年以后扩展为 2016–2026，共 117 款：iPhone 40、iPad 34、MacBook 43。
- 由“一行一个固定芯片”重构为“一行一个机型、行内选择官方芯片与合法的内存 + 存储组合”。
- 134 个实际被产品引用的芯片 / GPU 配置全部具有 CPU 单核、CPU 多核和 GPU 百分比，没有空分。
- CPU 单核、CPU 多核、GPU 拆成独立列；表头直接写明 100% 基准，固定基准百分比与条形视觉比例各司其职。
- 切换芯片后，三项性能、合法内存 / 容量、动态规格和二手参考价立即联动。
- 新增 Apple 官方规格直达链接、完整规格弹窗、配置快照对比、搜索与年份 / 类别 / 排序筛选。
- 新增独立的 `technical-report.html`，把公式、权重、数据源、估算和异常值处理集中说明；产品表不显示原始分、来源或可信度标签。

## 2. 2021 MacBook Pro 的修正

旧结构把不同芯片拆成多个产品行，而且漏掉官方选项。现在严格按 Apple 中国技术规格页建模：

| 机型 | 芯片选项 | 内存 | 存储 |
|---|---|---|---|
| MacBook Pro 14 英寸（2021） | M1 Pro 8CPU/14GPU、10CPU/14GPU、10CPU/16GPU；M1 Max 10CPU/24GPU、10CPU/32GPU | Pro 16/32GB；Max 32/64GB | 512GB、1/2/4/8TB |
| MacBook Pro 16 英寸（2021） | M1 Pro 10CPU/16GPU；M1 Max 10CPU/24GPU、10CPU/32GPU | Pro 16/32GB；Max 32/64GB | M1 Pro 512GB–8TB；M1 Max 1–8TB |

14 英寸是 5 个芯片档，16 英寸是 3 个。没有把 14 英寸的 8/14 或 10/14 错放进 16 英寸。

规格弹窗还会随芯片更新：M1 Pro / Max 的内存带宽、外接显示器数量；16 英寸的 M1 Pro / Max 重量分别为 2.1kg / 2.2kg；14 英寸最低档显示 67W 电源，其他档显示 96W。

官方依据：

- <https://support.apple.com/zh-cn/111902>
- <https://support.apple.com/zh-cn/111901>

## 3. 百分比如何计算

所有最终百分比都由原始 benchmark 输入在浏览器中实时计算，不在产品行里手写。

固定基准：

- iPhone：A14 Bionic = 100%
- iPad：M1（iPad）= 100%
- Mac：M1（8CPU/8GPU）= 100%

每一个测试项先归一化：

`ratio_i = candidate_raw_i / baseline_raw_i`

再做加权几何平均：

`index = 100 × exp( Σ(w_i × ln(ratio_i)) / Σw_i )`

这样处理的原因：Geekbench、Cinebench、PassMark、3DMark、FLOPS 的量纲完全不同，不能直接把原始数字相加；几何平均也能降低单项异常高分对总分的拉动。

当前权重：

- iPhone / iPad CPU 单核、多核：同平台 Geekbench 6，各自独立归一化。iOS 没有同版本 Cinebench，强行混入桌面测试反而不严谨。
- Mac CPU 单核：Geekbench 6 60%、Cinebench 2024 30%、PassMark Single 10%。
- Mac CPU 多核：Geekbench 6 55%、Cinebench 2024 30%、PassMark CPU Mark 15%。
- GPU：Geekbench 6 Compute 45%、可比的同版本 3DMark 35%、FP32 20%。
- 某一测试确实缺失时，在剩余实测项内重新归一化权重，缺项不等于 0 分。

## 4. 是否只用了 NanoReview

没有。NanoReview 是重要的聚合入口，但没有采用其算法不透明的 Final Score。实际校准还使用：

- Geekbench Browser 的 Mac、CPU、Metal / OpenCL 聚合；
- Cinebench 2024 的同版本 CPU / GPU 渲染项；
- PassMark 的老 Intel 大样本单核与 CPU Mark；
- 同版本 3DMark 项；
- Blender 或设备实测作为异常检查；
- FP32 只作低权重理论校验；
- Apple 技术规格页确认核心数和合法配置。

不同来源只比较同一测试版本。同一个芯片若某来源相对值与其他来源中位数偏离约 18% 以上，会先检查 API、测试版本、散热和样本；确认异常后降权或隔离。

已实际处理的离群例子：M1 Pro 的某些 Cinebench GPU 聚合相对 M1 出现约 4.75 倍，但 Geekbench Compute、3DMark、Blender 与 FP32 多数落在约 1.8–2.1 倍。该 Cinebench 项没有进入最终 GPU 指数。

## 5. 没有对应跑分的芯片怎样保证有分数

处理优先级：

1. 找具体设备、具体核心档的 Geekbench / Compute 聚合；
2. 找同档位第二来源或相邻档实测；
3. 同架构降配 GPU 用相邻档校准，实测 GPU 项按 `核心比例^0.93` 缩放，FP32 按核心比例；
4. 少 CPU 核心档保持同代单核实测，多核按性能核心数量、相邻档与机型功耗档校准；
5. 老 Intel 优先对应 Mac 型号 GB6，PassMark 只作跨代校准；缺 Cinebench 时对已有实测权重重归一化；
6. 最终百分比仍统一走固定公式，不直接写一个“看起来差不多”的数字。

例如 2021 14 英寸 M1 Pro 8CPU/14GPU 已采用具体设备聚合：Geekbench 6 单核 2184、多核 11159、OpenCL 35235；其余缺项由同族相邻档校准。因此它不是简单拿 10/16 档乘以 8/10 或 14/16。

## 6. 2016–2020 Intel Mac

旧 Intel 数据额外做了以下处理：

- 采用 Mac 实机 GB6 聚合，避免同名 CPU 在 Windows 台式散热条件下的分数直接套用；
- 录入 PassMark Single / CPU Mark 作为低权重跨代锚；
- 有可核验的 Cinebench 2024 才录入，没有就不伪造原始分；
- Intel 核显、Radeon Pro 450/455/460、555/560、555X/560X、Vega 16/20、5300M/5500M/5600M 分档计算；
- 15/16 英寸机型不再只列几个常见整机 SKU，也不再把 CPU 与独显做全排列，而是按 Apple 起配路径保留官方 bundle：2016 15 英寸 7 组、2017 15 英寸 5 组、2018/2019 15 英寸各 9 组、2019 16 英寸 11 组；
- 2020 13 英寸 Intel 同时保留双雷雳 3 与四雷雳 3 两个官方机型；
- 不混用 3DMark 11、Fire Strike、Time Spy 的不可比原始分。

## 7. 官方规格口径

- 产品以首次发布年份 2016–2026 纳入，早期机型使用 Apple 中国技术规格直达页。
- 官方没写刷新率时显示“Apple 未标注 Hz”；旧 iPad Pro 页面只写 ProMotion 时不擅自补 120Hz。
- Apple 不公开 iPhone 额定 Wh，因此 iPhone 统一显示官方视频 / 流媒体最长时长，不混入拆机容量。
- iPad 与 Mac 官网公开 Wh 时显示容量；iPad 同时保留 Wi‑Fi 使用最长 10 小时的官方口径。
- M1/M2/M4/M5 iPad Pro 的容量—CPU—内存绑定为不可拆选项。例如 M4 256/512GB 是 9 核 CPU + 8GB，1/2TB 是 10 核 CPU + 16GB；切换容量对应性能也会改变。
- M2/M3/M4/M5 Pro 与 Max 改为逐芯片档声明内存和存储：Max 不再错误出现 512GB，M3/M4 Pro 不再出现 8TB，M5 Pro 为 1–4TB、M5 Max 为 2–8TB；低阶 Max GPU 档也不会混入高阶档专属内存。
- 屏幕、分辨率、刷新率、重量、电池、接口、亮度、相机、无线和外接显示器等重要字段保留在完整规格弹窗；默认结果表只留下选购时最常用的信息。

## 8. 二手价格联动

价格专项审计判定旧模型不合理，已经废弃：

- 117 款基础数据的转转中点平均是闲鱼的 85.08%；全部 iPad 与 Mac 的转转两端几乎都等于闲鱼乘 0.85，并非独立平台采样。
- 旧公式让 28 个高芯片档把绑定内存 / 存储与芯片差价重复相加，中位额外多算 28.35%。
- 清理 Intel 非法组合前枚举的 1,273 个配置中有 8 个出现 0 元端点；老款 4TB / 8TB 配置会被推到明显不可能的两万元以上。
- 转转较低的数字更像平台回收价，但页面链接却指向买家购买搜索，语义混乱。

新模型改为：

- “闲鱼个人”表示正常自用机的个人挂牌 / 近期成交估值；“转转验机”表示带验机和售后服务的买家零售估值，前台以 `≈` 标示；平台回收价不再混入。
- 28 个代表性默认 SKU 使用 2026 年公开交易样本建立直接价格锚点；其余默认配置按同类别、同系列、相近年份锚点加权校准。
- 芯片档差价按机龄保留 15%–62%；组内内存 / 存储差价单独折旧。每个芯片组都以自己的默认合法组合为锚，避免 Max 或 iPad Pro 绑定容量重复计价。
- 上下限改在“中点 + 相对离散率”上生成，全部按 50 元取整且必须大于 0；高配不会因为固定加价而虚假地缩窄区间。
- 转转缺少可公开复核的同 SKU 独立样本时，用闲鱼中点加 iPhone 10%、iPad 11%、Mac 12% 的服务溢价生成明确的模型估值，不再宣称为实测值。
- 点击价格仍会把当前机型、芯片、内存和容量带入平台搜索词，供购买当天复核。

代表修正：iPhone 15 Pro 512GB 从闲鱼 4550–6100 改为 3950–4450；iPad Pro 13 M4 16GB+1TB 从 13250–15550 改为约 8500–10500；16 英寸 M1 Max 32GB+1TB 从 14400–16800 改为约 7050–8200；M1 Air 16GB+512GB 从 3000–4300 改为 2900–3500。完整样本、公式、边界和来源写入 `technical-report.html`。

## 9. 展示与交互改造

- 删除首屏“3 项 / 2016–2026 / 官方规格 / 全覆盖”等自证卡片，页头只保留标题、用途说明和一个低调的“方法说明”入口。
- 搜索、iPhone / iPad / MacBook、年份、系列和排序成为第一操作区；系列由一排按钮收敛为单个下拉，不再让用户先理解页面结构。
- 大屏默认 7 列表格：机型、芯片、目标规格、单核、多核、GPU、二手参考。屏幕、重量、电池和接口等移入规格弹窗。
- “规格 / 对比 / Apple”放在机型旁边；闲鱼与转转合为一个二手参考单元格，对比入口不再藏到表格最右侧。
- 手机窄屏自动使用精简卡片，桌面自动使用表格，删除“表格 / 卡片”选择；移动筛选采用两列加一行排序，不产生横向滚动条。
- 顶部年份、系列、排序统一为同一套带可见标签的选择控件；芯片与目标规格使用更高、更清楚的主选择样式，并补齐 hover、键盘焦点、自定义箭头和高对比度模式。
- 只有一个合法选项时直接显示普通文字，不渲染下拉框，也不再用边框、灰底和圆角把静态值画成“假选择框”；手机端真正可选的控件统一为 44px 高、16px 字号，避免误触和 iOS 聚焦放大。
- 当前配置偏离默认时，只高亮真正发生变化的芯片或目标规格；当前分类出现改配后，筛选栏才显示“复位配置”和改配数量，默认状态不额外占用注意力。
- “复位配置”覆盖当前 iPhone / iPad / MacBook 分类的全部机型，不受年份、系列或搜索隐藏影响；它保留筛选、排序、其他分类配置和已经保存的对比快照。
- 表头直接写明各列 100% 基准，结果标题旁只保留一行短说明，不再重复三枚颜色徽章。
- 数字百分比始终相对于固定芯片基准。条形长度则在当前筛选结果内按单核、多核、GPU 三列分别归一化：`bar_width = score / current_column_max × 100%`。因此每列第一名都满格，GPU 的高倍率不会压扁单核或多核。
- 筛选、搜索或切换芯片后重新计算三列最大值；排序、完整规格和并排对比仍使用固定基准数字，不会被视觉归一化污染。
- 目标规格由两个独立下拉改为单一组合下拉，例如 `16GB + 512GB`；只展示该芯片档合法的内存—存储搭配，避免 iPad Pro 出现跨档混选。
- 相同芯片但存在多组绑定规格时合并为一个芯片选项：例如 M1/M2 iPad Pro 的芯片只出现一次，目标规格组合中再列 8GB + 128/256/512GB 与 16GB + 1/2TB。
- 切换芯片或目标规格组合后，原地重绘性能、价格和动态规格。
- 对比栏保存“当前配置快照”，同一机型可用不同芯片 / 容量分别加入对比。
- 原始跑分、来源、估算标签不在产品前台展示；只在技术报告解释。

## 10. 验证结果

自动数据检查 `node verify.js`：

- 117 款产品，年份最小 2016、最大 2026；
- iPhone 40、iPad 34、MacBook 43；
- 134 个被引用芯片 / GPU 配置全部存在；
- 每个配置均有正数 CPU GB6 单核、CPU GB6 多核、GPU Compute 和 FP32 输入；
- 1,242 个合法目标配置的闲鱼 / 转转区间全部为有限正数、上下限有序；旧版 8 个 0 元端点已全部消失；
- 没有任何配置继续落在“转转中点 = 闲鱼中点 × 0.85 ± 0.5%”的旧机械关系；
- 全量闲鱼最低端点 100 元、最高端点 31,200 元，旧 Intel 稀有高配不再无上限叠加；
- 2021 14 英寸严格 5 档，16 英寸严格 3 档；
- iPhone 16e 正确使用 A18 4GPU，不再误写 A18 Pro；
- 2026 iPhone 17e、11/13 英寸 iPad Air M4 已纳入；
- iPhone 没有伪装成官方 Wh，未标注刷新率没有硬写 60Hz。

浏览器交互验收：

- 页面无控制台错误；
- 桌面默认 7 列且页面无横向溢出；手机自动切为卡片，筛选区和页面均无横向溢出；
- 桌面筛选控件高 38px；390px 手机端筛选、芯片和规格控件均为 44px，三项筛选都有可访问名称；只有一个芯片档的机型显示静态值。
- 按闲鱼低价排序时，把 iPhone XS 从 512GB 改为 64GB 后会按新价格移动；即使随后被搜索隐藏，复位按钮仍能恢复 512GB、原始价格和原排序位置。
- 按多核排序时，把 2021 14 英寸 MacBook Pro 从 M1 Pro 8/14 改为 M1 Max 10/32 后会移动到首位；复位后恢复 M1 Pro 8/14 和原顺序，搜索与排序保持不变。
- iPad 与 iPhone 的改配状态按大分类隔离；复位 iPad 不会清除 iPhone 改配。复位也不会修改已保存的配置对比快照。
- M5 Pro 14 英寸默认恢复为 24GB + 1TB；切到 32 核 GPU M5 Max 后只出现 36GB + 2/4/8TB，不再生成不存在的 512GB 或 64/128GB 组合。
- iPhone 当前结果的单核、多核、GPU 三列最大条宽均为 100%；筛到单一机型时三条都为 100%，旁边固定基准数字保持不变；
- MacBook 分类显示 43 款；
- 2021 14 / 16 英寸下拉分别为 5 / 3 项；
- 14 英寸从 M1 Pro 8/14 切到 M1 Max 10/32 后，单核、多核、GPU 和价格全部变化；
- 512GB 切到 8TB 后，二手参考区间再次变化；
- 完整规格、配置快照对比和技术报告均能正常打开。
- 目标规格已经不存在独立的内存 / 存储选择器；M1 iPad Pro 合并为 1 个芯片项、5 个合法组合，M4 iPad Pro 两个芯片档分别只显示 2 个合法组合。
- 16 英寸 M1 Max 32GB+1TB 显示闲鱼约 7,050–8,200；13 英寸 M4 iPad Pro 16GB+1TB 显示约 8,500–10,500，均覆盖审计实价锚点；
- 页面将“闲鱼个人”与“转转验机”明确分开，转转估值带 `≈`，技术报告可正常打开且无控制台错误。

## 11. MacBook Pro 配置级整机规格专项审计

本次继续审计“同一 MacBook Pro 换芯片后，除了性能以外还有哪些东西会变”。结论是旧实现只部分处理了 2021 M1，并不完整：M2–M5 多数仍把多个重量、接口或电池值揉进一个静态字符串，Intel 独显款甚至把 CPU 与 GPU 做成全排列。

数据结构已改成三层合并：`整机固定规格 + 芯片固有规格 + 当前机身×芯片档规格`。芯片固有层保存内存带宽与媒体引擎；配置层保存重量、官方续航、随附电源、端口、外接显示器、显存和当前芯片对应的 Apple 链接。详情、搜索和配置快照对比全部读取同一个解析结果，删除 2021 机型的零散 ID 特判。

主要修正：

- 14 英寸 2023 的基础 M3 改用 Apple `/117735`，Pro/Max 使用 `/117736`；14 英寸 2024 的基础 M4 改用 `/121552`，Pro/Max 使用 `/121553`。切芯片时 Apple 链接同步变化。
- 14 英寸 M4 会在 1.55kg / 1.60kg / 1.62kg、TB4 / TB5、最多 2 / 4 台外显、120 / 273 / 410 / 546GB/s 之间准确联动；续航和 70W / 96W 电源也随具体 CPU 档更新。
- M5 Pro/Max 补齐 307 / 460 / 614GB/s、单 / 双视频编码与 ProRes 引擎、14 英寸 1.60 / 1.62kg、最多 3 / 4 台外显。CPU 核心文案按 Apple 改为“超级核心 + 性能核心”，不再误写成性能 + 能效。
- 16 英寸 2021 的 M1 Max 改为 1TB 起，删除不存在的 512GB 组合；价格仍以各芯片组自己的最低合法配置为锚，不重复收取强制 1TB 差价。
- 2016–2019 五款 Intel 独显 MacBook Pro 从 48 个 CPU×GPU 全排列改为 Apple 官方 41 个 bundle：删除 10 个不存在的组合，新增三个 CPU 各自对应的 Radeon Pro 5500M 8GB GDDR6 档，并让显存和最低 SSD 随 bundle 联动。
- 2020 双雷雳 3 机型的官方链接从错误的 `/111339` 改为 `/111981`；2019 16 英寸内屏刷新率改为 Apple 公布的 47.95 / 48 / 50 / 59.94 / 60Hz。
- Intel 同一官方机型的重量、端口、电源与外显保持整机固定。Apple 只说明重量可能因配置略有差异，并未发布逐 BTO 数字，因此网站不做猜测。

自动检查现在还会逐档确认 Apple Silicon MacBook Pro 必须具备重量、电池、续航、电源、端口、外显、带宽和媒体引擎；确认基础 M3/M4 的配置级官方链接；确认 M5 核心组成；确认 Intel 合法 bundle 数、5500M 8GB 三档及被禁止组合不存在。最终为 117 款机型、134 个被引用芯片 / GPU 配置、1,242 个官方合法目标配置。

## 12. 文件结构

- `index.html`：产品对比主页面与弹窗结构
- `styles.css`：表格、卡片、响应式、对比与报告视觉样式
- `data.js`：产品、官方配置、benchmark 原始输入与来源元数据
- `app.js`：评分、筛选、配置联动、价格修正、详情与对比逻辑
- `technical-report.html`：对外可读的性能评分与二手价格技术报告
- `verify.js`：零依赖数据完整性检查
